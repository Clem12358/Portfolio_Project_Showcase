# Portfolio Project Showcase

Streamlit app that reproduces your dashboard performance view with frozen data (no live DB link).

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Pages

- `app.py`: performance KPIs + charts
- `pages/2_Portfolio_Holdings.py`: month selector with full holdings and weights (frozen snapshot)
